<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :category_title %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :category_icon %>
  </div>

  <div class="field">
    <%= form.label :tag %>
    <%= form.number_field :tag, id: :category_tag %>
  </div>

  <div class="field">
    <%= form.label :parent_id %>
    <%= form.number_field :parent_id, id: :category_parent_id %>
  </div>

  <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published, id: :category_published %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :category_slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
